{
  "name": "@isitagentready/core",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "fast-xml-parser": "^5.3.0",
    "@isitagentready/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
